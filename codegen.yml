overwrite: true
schema: "https://shaya.liara.run/graphql"
documents: "src/**/*.graphql"
generates:
  src/generated/graphql.ts:
    plugins:
      - "typescript"
      - "typescript-operations"
      - "typescript-apollo-angular"
      - "fragment-matcher"
  ./graphql.schema.json:
    plugins:
      - "introspection"
